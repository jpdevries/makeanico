<div class="export widget">
  <div class="neat container">
    <div class="svg-preview">
      {% include '../favicon-preview.twig' with cells %}
    </div>
    <h2 id="export">Export</h2>
    <p>
      Your <i><span class="filename">favicon</span><span class="exp"></span></i> icon is ready for&nbsp;download.
    </p>
    <div id="dlname"></div>
    <p></p>
    <form action="/export" method="post" class="download-as">
      {% if includeCellURL|default(false)  %}<input type="hidden" name="cellurl" value="{{ cellURL }}" />{% endif %}
      <fieldset>
        <legend>Download As</legend>
        <label for="download__ico">
          ICO
          <input type="radio" id="download__ico" name="download__as" checked value="ico">
        </label>&emsp;&emsp;
        <label for="download__svg">
          SVG
          <input type="radio" id="download__svg" name="download__as" value="svg">
        </label>&emsp;&emsp;
        <label for="download__png">
          PNG
          <input type="radio" id="download__png" name="download__as" value="png">
        </label>
        <button type="submit">Download Favicon</button>
      </fieldset>
    </form>
    <div id="jsbts"></div>
  </div>
</div>
