<g class="art">
  {% for row in cells %}
      {% for cell in row %}
        {% if cell.fill %}
          <rect x="{{ cell.column }}" y="{{ cell.row }}" width="1" height="1" fill="{{ cell.rgb }}" opacity="{{ cell.opacity }}"></rect>
        {% endif %}
      {% endfor %}
  {% endfor %}
</g>
